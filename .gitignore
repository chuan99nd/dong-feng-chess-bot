# Secrets — token for deploy/commit-model.yml push (NEVER commit)
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.ruff_cache/
.pyright/
.mypy_cache/
.coverage
htmlcov/

# Project artifacts (root-anchored so they don't match the src/dongfeng/data package)
/data/
/checkpoints/
/runs/
# Models live on the Hugging Face Hub (deploy/push-model.yml), not git.
/models/
# Monitoring state mirror (Prometheus TSDB + Grafana DB) pulled by deploy/backup.yml
/monitoring/

# Engines / nets (large binaries)
*.nnue

# Deploy secrets
deploy/inventory.ini
deploy/*.vault
deploy/secrets/
# Cloudflare Tunnel credentials (never commit)
*.pem
cert.pem
**/.cloudflared/
deploy/cloudflared/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
.claude/worktrees/
